import React from 'react';
import LoginPage from '../components/loginPage/LoginPage';

const Login = () => {
    return (
        <div>
            <LoginPage />
        </div>
    );
};

export default Login;
